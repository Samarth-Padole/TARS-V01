GPIO0   → BOOT
GPIO1   → TOUCH_TOP
GPIO2   → TOUCH_LEFT
GPIO3   → RESERVED

GPIO4   → CAM_D0
GPIO5   → CAM_D1
GPIO6   → CAM_D2
GPIO7   → CAM_D3
GPIO8   → TOUCH_RIGHT
GPIO9   → CAM_D4
GPIO10  → CAM_D5
GPIO11  → CAM_D6
GPIO12  → CAM_D7
GPIO13  → CAM_PCLK
GPIO14  → CAM_VSYNC
GPIO15  → CAM_HREF
GPIO16  → CAM_XCLK
GPIO17  → I2C_SDA / CAM_SCCB_SDA
GPIO18  → I2C_SCL / CAM_SCCB_SCL
GPIO19  → USB_D-
GPIO20  → USB_D+
GPIO21  → TFT_SCLK

GPIO22–25 → NOT USED
GPIO26–37 → RESERVED

GPIO38  → TFT_MOSI
GPIO39  → TFT_CS
GPIO40  → TFT_DC
GPIO41  → TFT_BL
GPIO42  → I2S_BCLK
GPIO43  → I2S_DOUT
GPIO44  → I2S_DIN
GPIO45  → RESERVED
GPIO46  → RESERVED
GPIO47  → I2S_WS
GPIO48  → RGB_LED
